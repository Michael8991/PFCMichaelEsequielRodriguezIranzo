<?php 
// session_start();

// if (!isset($_SESSION['user'])) {
//     header("location: login.php");
//     exit;
// }

// require '../conexion.php'; 
// include '../BudgetOPs/pageBudget.php';

// // $searchTerm = isset($_GET['searchTerm']) ? $_GET['searchTerm'] : '';

// // Llamar a la función getRecords y obtener los registros
// $records = getRecords($conn, $items_per_page, $offset, $company_id, $searchTerm);

// // Devolver los registros como respuesta JSON
// header('Content-Type: application/json');
// echo json_encode($records);
//  // Terminar el script de PHP

?>